[build]
  command = "npm run build"
  publish = "dist"

# Single-page-app routing: send every path to index.html so React Router
# can handle /listing/:id, /admin, /login, etc.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
